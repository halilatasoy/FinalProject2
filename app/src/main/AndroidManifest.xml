<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalProject2"
        tools:targetApi="31">

        <!-- SignInActivity tanımı ve varsayılan başlangıç aktivitesi olarak ayarlanması -->
        <activity android:name=".SignInActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- SignUpActivity tanımı -->
        <activity android:name=".SignUpActivity"
            android:exported="true" />

        <!-- BudgetActivity tanımı -->
        <activity android:name=".BudgetActivity"
            android:exported="true">
            <!-- Diğer aktiviteler burada tanımlanabilir -->
        </activity>

        <activity android:name=".TransactionListActivity"
            android:exported="true" />

    </application>
</manifest>
